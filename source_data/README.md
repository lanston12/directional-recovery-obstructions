# Source-data index

The public source-data archive is attached to https://github.com/lanston12/directional-recovery-obstructions/releases/tag/v1.0.0.

The authoritative item-by-item mapping is [`../SOURCE_DATA_README.md`](../SOURCE_DATA_README.md). Numerical files are not duplicated or altered in this directory. They remain in `../results/` and `../derived_results/`; the reproducible `source_data.zip` archive collects those unchanged files for submission upload.
